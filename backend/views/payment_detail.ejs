<!DOCTYPE html>
<html>
  <head>
    <title></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <%- include layout %>
    <div class="container">
        <h2 align = "center">เขียนบทความ</h2>
        <% if(locals.errors) { %>
        <ul class = "alert alert-danger">
            <% Object.values(errors).forEach(function(error){ %>
                <li><%= error.msg %></li>
            <% }) %>
        </ul>
        <% } %>
        <form class ="" action="" method="post">
            <div class="form-group">
                <label for="">tutor name</label>
                <input type="text" name="tutor" value="" class="form-control">
            </div>
            <div class="form-group">
                <label for="">price</label>
                <textarea name="price" rows="8" cols="80" class="form-control"></textarea>
            </div>
            <input type="submit" name="" value="บันทึก" class="btn btn-primary">
        </form>
    </div>
  </body>
</html>